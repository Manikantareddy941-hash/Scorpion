version: '3.8'

services:
  loki:
    image: grafana/loki:3.7.3
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - scorpion-net

  tempo:
    image: grafana/tempo:latest
    command: ["-config.file=/etc/tempo.yaml"]
    volumes:
      - ./tempo.yaml:/etc/tempo.yaml
    ports:
      - "4318:4318"   # OTLP HTTP
      - "3200:3200"   # Tempo query
    networks:
      - scorpion-net

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      # rule_files in prometheus.yml resolves relative to --config.file, so the
      # rules must land in the same directory. Without this mount Prometheus
      # fails to start rather than starting without alerts, which is the right
      # failure: a missing rule file should not look like a quiet system.
      - ./prometheus.rules.yml:/etc/prometheus/prometheus.rules.yml
    networks:
      - scorpion-net
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/console_templates'

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - scorpion-net
    depends_on:
      - loki
      - prometheus
      - tempo

networks:
  scorpion-net:
    driver: bridge

volumes:
  grafana-data:
